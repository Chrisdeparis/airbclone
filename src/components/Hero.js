import React from "react";

function Hero() {
  return (
    <div className="hero">
      <nav>
        <h2>Welcome</h2>
        <button>Logout</button>
      </nav>
    </div>
  );
}

export default Hero;
